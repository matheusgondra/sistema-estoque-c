{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build do projeto",
            "type": "shell",
            "command": "make clean && make",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Compila o projeto usando o Makefile"
        }
    ]
}